version: 1.0
runtime: php81
build:
  commands:
    build:
      - chmod +x startup.sh
      - cat /etc/apprunner/php81/nginx-httpd-setup.sh
run:
  command: ./startup.sh
  network:
    port: 8080
    env: APP_PORT
